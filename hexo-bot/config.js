/**
 * Hexo QQ Bot 配置
 * 请根据实际情况修改以下配置
 */
module.exports = {

  // ─── Hexo 配置 ───────────────────────────────────────────────────────────
  /** Hexo 根目录（不带末尾斜杠） */
  hexoPath: 'D:/hexo',

  // ─── 发布行为 ────────────────────────────────────────────────────────────
  /**
   * 发文后自动执行 hexo deploy（true = 发一篇立即推送到 GitHub Pages）
   * 设为 false 则只存本地文章，稍后手动执行 npm run deploy
   */
  autoDeploy: false,

  /**
   * 存入草稿后自动发布草稿到 _posts（需用 /puball 命令触发）
   */
  autoPublishDraft: false,

  // ─── 安全设置 ────────────────────────────────────────────────────────────
  /**
   * 允许使用机器人的 QQ 号列表
   * 留空 = 不限制，所有联系机器人的用户都能发文
   * 示例: ['123456789', '987654321']
   */
  allowedUsers: [],

  /**
   * 允许接收消息的群号列表（群聊模式）
   * 留空 = 所有群都能使用
   */
  allowedGroups: [],

  /**
   * 机器人 QQ 号（用于识别 @ 消息，不需要可填空）
   */
  qqNumber: '9187541',

  /**
   * 机器人 QQ 密码（留空则使用扫码登录）
   */
  password: '',

  // ─── go-cqhttp 连接配置 ──────────────────────────────────────────────────
  /** 本服务 WebSocket 监听端口（go-cqhttp 反向 WS 连接地址） */
  wsPort: 8088,
};
